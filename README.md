# langfuse-helm-chart
Langfuse Helm Chart deploys Langfuse on Kubernetes with all core dependencies. It includes PostgreSQL for metadata, ClickHouse for analytics, MinIO for S3-compatible object storage, and Valkey (Redis-compatible) for caching and queues. Enables a scalable, production-ready setup.
